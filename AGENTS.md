# Agent Instructions

## 命名规范

所有配置目录和字段遵循 **Unix 风格命名**：

- **单数** — 目录名用单数，表示"这一类对象的定义"。好：`pipeline/`，不好：`pipelines/`
- **小写** — 全小写字母。好：`blueprint/`，不好：`BluePrint/`
- **连字符** — 单词间用连字符分隔。好：`contract/`，不好：`contracts/`
- **无缩写** — 除非是通用缩写，否则不缩写。好：`catalog/`，不好：`catlg/`

这条规则适用于此仓库中所有目录名、文件名和配置字段。
